import styled from 'styled-components';

export const UpSideLeftBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 16px;
  top: 399px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const UpSideMiddleBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 134px;
  top: 399px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const UpSideRightBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 252px;
  top: 399px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const MiddleSideLeftBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 16px;
  top: 458px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const MiddleSideMiddleBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 134px;
  top: 458px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const MiddleSideRightBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 252px;
  top: 458px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const DownSideLeftBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 16px;
  top: 517px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const DownSideMiddleBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 134px;
  top: 517px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;

export const DownSideRightBox = styled.div<{ active: boolean }>`
  box-sizing: border-box;

  position: absolute;
  width: 107px;
  height: 48px;
  left: 252px;
  top: 517px;

  background: #ffffff;
  border: solid;
  border-width: 1px;
  border-color: ${({ active }) => (active ? '#F4851E' : '#767676')};

  display: flex;
  justify-content: center;
  align-items: center;

  font-family: 'Pretendard';
  font-style: normal;
  font-weight: 400;
  font-size: 14px;
  line-height: 17px;
  /* identical to box height */
  color: ${({ active }) => (active ? '#F57906' : '#191919')};

  cursor: pointer;
`;
